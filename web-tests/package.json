{
  "name": "media-optimizer-web-tests",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Verifies the injected client script against real jellyfin-web markup.",
  "scripts": {
    "test": "node dom.test.mjs && node lifecycle.mjs && node hostile.mjs && node render.mjs && node dashboard.mjs && node movepage.mjs && node encoders.mjs && node analysis.mjs && node config.mjs"
  },
  "devDependencies": {
    "jsdom": "^25.0.0",
    "playwright": "^1.48.0"
  },
  "dependencies": {
    "playwright": "^1.62.1"
  }
}
